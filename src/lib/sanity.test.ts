import { describe, expect, it } from 'vitest'

// Test minimal qui vérifie seulement que Vitest fonctionne.
// À remplacer par de vrais tests (ex. oneRepMax.test.ts) dès qu'il y aura de la logique dans src/lib/.
describe('outillage de test', () => {
  it('exécute un test', () => {
    expect(1 + 1).toBe(2)
  })
})
